# MediumClass 编译与交付（维护者使用）

普通玩家请打开 [00-先看这里.txt](00-先看这里.txt)。他们安装前置、提供参考文件和反馈测试结果即可。

## 当前状态

尚未编译出维护版安装包。2026-09-24 的玩家日志是 `MediumClass.dll not found`：UMM 读取了 Info.json，但没有执行 MediumClass 的代码。这不是一次有效的职业功能测试。

参考程序集尚未收到；目标为 WotR PC 2.7.0x、UMM 0.33.0、ModMenu 2.0.14、TTT-Core 0.7.14a。BlueprintCore 2.8.7 是构建依赖，需要合并进最终 DLL。

## 一、准备构建机

- Windows + VS 2022 或 Build Tools，安装 .NET 桌面构建工具/MSBuild 和 .NET Framework 4.7.2 targeting pack。
- 首次还原 NuGet 需要网络。
- 获取个人 Fork 的 `maintenance/wotr-2.7-offline` 最新源码；找到包含 MediumClass.csproj 的目录。不要把源码放进游戏 Mods。
- 用目标游戏与依赖 DLL 作为引用；收到收集 ZIP 时保留清单用于版本和哈希核对，不将参考 DLL 提交到 GitHub。

## 二、配置路径

在 Developer PowerShell for VS 2022 中进入源码根目录：

```powershell
Copy-Item .\MediumClass.local.props.example .\MediumClass.local.props
notepad .\MediumClass.local.props
```

将 WrathPath 改成游戏主目录，例如：

```xml
<WrathPath>D:\steam\steamapps\common\Pathfinder Second Adventure</WrathPath>
```

收到参考 ZIP 后，也可以把 WrathPath 指向解压后的参考根目录（里面有 Wrath_Data 和 Mods）。只将这个目录用于构建，不要对参考副本运行 DeployMod。

若实际依赖目录与默认值不同，设置 ManagedPath、UMMPath、ModMenuPath、TTTCorePath。含 `&` 等 XML 特殊字符的路径须正确转义。保留完整构建输出，出现错误时不交付产物目录里的历史 ZIP。

## 三、构建、合并、打包

```powershell
.\scripts\Build.ps1 -Target PackageMod
```

显式 PackageMod 会先 Build，再由 ILRepack 合并 BlueprintCore，暂存三个文件并生成：

```text
artifacts\Release\MediumClass-UMM-install-untested.zip
```

只需看暂存文件时使用 `-Target StageMod`，输出为 `artifacts\Release\MediumClass`。普通 Build 不执行合并或安装。

如果命令退出失败，停止交付并处理完整错误。旧输出可能仍存在，不能因为目录里有 ZIP 就认为本次构建成功。

## 四、交付前核验

1. 构建和 ILRepack 均明确成功，记录源码提交、引用版本、构建日志和最终 DLL/ZIP 的 SHA-256。
2. 安装 ZIP 只含 MediumClass.dll、Info.json、mediumclass_assets。确认是本次合并后的 DLL；仅有 Info.json 不代表可运行。
3. 用程序集检查工具核对 DLL 中包含 BlueprintCore 类型，AssemblyRef 不再要求外置 BlueprintCore；检查资源和入口名。
4. 不得夹带游戏程序集、UMM/TTT/ModMenu DLL、参考 ZIP 或源码。玩家说明单独交付，Mod stage 保持三文件。
5. 随安装包明确说明未实机验证的项目。不要发布正式版本或提交上游 PR，除非用户另行授权。

构建验证通过后，普通玩家按 [01-安装与测试说明.md](01-安装与测试说明.md) 第四部分直接用 UMM 安装。旧文件夹可能名为 Medium Class 或 MediumClass，先移出 Mods 备份，避免两个实例。

## 五、技术测试与未解决项

详细分类与存档风险见 [再次自查](maintenance/SELF-REVIEW-2026-09-23.zh-CN.md) 和 [本轮复查](maintenance/REVIEW-2026-09-24.zh-CN.md)。优先顺序：冷启动 → 新角色 → 六灵体存读/休息 → 双 Medium 与共享祝福 → 升级和洗点 → 旧档副本。

特别记录：通灵扣费跨阈值时序、Source 身份、Spirit Surge/Trance 运行时状态、Arcane Surge 原费用恢复、离队受益者，以及 Hierophant/Marshal 旧 TypeId 状态。不自行重写 Trickster’s Edge 公式或启用原本未挂载的功能。
